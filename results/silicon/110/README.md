# Si(110) GPAW result provenance

The production event table ends at **109 atoms**. The archived
`grafeno_cutoff_checkpoint.xyz` also contains 109 atoms and is therefore the
structure used by the manuscript figure pipeline.

The original PAPER tree also contained a file named `grafeno_cutoff_final.xyz`
with **82 atoms**. It is preserved here as
`grafeno_cutoff_final_legacy_mismatch.xyz` for auditability, but it is **not**
used as the final state of this trajectory.
